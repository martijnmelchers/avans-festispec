
@{
    ViewData["Title"] = "Index";
}

@model IEnumerable<Festispec.Models.PlannedInspection>

@foreach (var item in Model)
{
    <div class="Inspection-row container">
        <!--<div class="row">
        <h2 class="col-6">@item.EventTitle</h2>
        <span class="col-4">@item.StartTime.ToShortDateString() @item.StartTime.ToShortTimeString() - @item.EndTime @item.EndTime</span>
        @Html.ActionLink("Open vragenlijst", "Details", new { id = item.Id }, new { @class="btn btn-primary col-2" })
    </div>-->
        <div class="card">
            <h5 class="card-title">@item.EventTitle</h5>
            <p class="card-text">@item.StartTime.ToShortDateString() @item.StartTime.ToShortTimeString() - @item.EndTime @item.EndTime</p>
            @Html.ActionLink("Open vragenlijst", "Details", new { id = item.Id }, new { @class = "btn btn-primary" })
        </div>
    </div>
}
