@model Festispec.Models.Answers.FileAnswer

<!--
 <div class="row">
    <div class="col-md-4">
        @Html.ActionLink("Tekenen", "Draw", "Draw", new { id = Model.Id }, new { @class = "btn btn-primary" })
    </div>
</div> -->



<h4>@Model.Question.Contents</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="SaveDrawAnswer">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input asp-for="PlannedInspection.Id" type="hidden" />
            <input name="QuestionId" type="hidden" value="@Model.Question.Id" />
            <div class="form-group">
                <input asp-for="@Model.Id" class="form-control" value="@Model.Id" type="hidden" />
            </div>
            <div class="form-group">
                <input type="submit" value="Tekenen" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
