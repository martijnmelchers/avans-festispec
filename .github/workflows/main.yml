name: PR Build

on: [pull_request]

jobs:
  build:

    runs-on: [windows-latest]
    steps:
      - name: Checkout
        uses: actions/checkout

      - name: Build UI project
        run: |
          cd src/UserInterface
          dotnet restore
          dotnet build
